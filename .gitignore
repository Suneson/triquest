# OS / editor cruft
.DS_Store
Thumbs.db
*.log
.vscode/
.idea/

# Node (no runtime deps, but keep this clean if any are ever added)
node_modules/
npm-debug.log*

# Local backups exported from the app
triquest-backup-*.json

# Pixel-art source files — ship only the exported PNGs, not the heavy PSDs
*.psd
